---
interface:
  display_name: "LLM Wiki Capture"
  short_description: >
    User-invoked capture or source ingest into my-llm-wiki.
  default_prompt: >
    Use $llm-wiki-capture to capture reusable knowledge from the current
    session, review candidates, or ingest provided source links into
    my-llm-wiki.
policy:
  allow_implicit_invocation: false
